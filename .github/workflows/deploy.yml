name: Build and Deploy

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Docker
      uses: docker/setup-buildx-action@v2

    - name: Log in to DockerHub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.khalilfatima612 }}
        password: ${{ secrets.03195251682Ff@ }}

    - name: Build and push Docker image
      run: |
        docker build -t yourusername/fake-news-api:latest .
        docker push yourusername/fake-news-api:latest

    
